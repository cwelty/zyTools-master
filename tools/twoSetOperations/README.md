## 2 set operations tool

This tool has 2 options:
    * |layPersonEdition|. Options is not needed. Default is |layPersonEdition| is false.
    * |useMultipleParts| - Optional and boolean. Default is false.