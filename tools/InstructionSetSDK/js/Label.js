/*
    A Label stores:
        |name| is required and a string.
        |instructionIndex| is required and a number.
*/
function Label(name, instructionIndex) {
    this.name = name;
    this.instructionIndex = instructionIndex;
}
