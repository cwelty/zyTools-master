/*
    RegisterNameAndValue stores:
        |name| is required and a string.
        |value| is required and a number.
*/
function RegisterNameAndValue(name, value) {
    this.name = name;
    this.value = value;
}
