/**
    Structure to store Token's name and value.
    @class Token
    @constructor
    @param {String} name Name of the token.
    @param {String} value The token's value.
*/
function Token(name, value) {
    this.name = name;
    this.value = value;
}
